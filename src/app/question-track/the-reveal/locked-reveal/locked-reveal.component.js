import template from './locked-reveal.html';

// Component which displays the truth when the user hasn't completed all the questions.

const LockedRevealComponent = {
  bindings: {},
  template
};

export default LockedRevealComponent;
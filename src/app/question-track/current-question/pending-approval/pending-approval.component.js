import template from './pending-approval.html';
import controller from './pending-approval.controller';

// Component which notifies the user that their answer must be approved.  

const PendingApprovalComponent = {
  bindings: {},
  template,
  controller
};

export default PendingApprovalComponent;
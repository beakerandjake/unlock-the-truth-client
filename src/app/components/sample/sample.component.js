import template from './sample.html';
import controller from './sample.controller';
import './sample.css';

// An example component that displays to the user a blessed image
const sampleComponent = {
    bindings: {},
    template,
    controller
};

export default sampleComponent;